# API の URL
API_URL=https://<YOUR_CLOUD_RUN_URL>/process-audio
# API トークン: バックエンドの API_TOKEN に合わせる
API_TOKEN=replace_with_secure_token
# 録音秒数 (push-to-talk 簡易版)
RECORD_SECONDS=4
# サンプルレート (Pi 側は 16000 を推奨)
SAMPLE_RATE=16000
# GCP 認証: Cloud Run ではサービスアカウントか Workload Identity を推奨
# ローカルでサービスアカウントキーを使う場合はパスを指定
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/key.json
